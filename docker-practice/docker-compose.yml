

services:
  # "web" 이라는 이름의 서비스(컨테이너)를 정의해요.
  web:
    # 현재 폴더(.)의 Dockerfile 을 사용해서 이미지를 빌드해요.
    build: .
    # 포트 매핑: 내 컴퓨터의 5000번 포트를 컨테이너의 5000번 포트와 연결해요.
    # 왼쪽이 내 컴퓨터, 오른쪽이 컨테이너 포트예요. (localhost:5000 으로 접속)
    ports:
      - "5000:5000"
